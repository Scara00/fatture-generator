import React from "react";

const Header = () => (
  <header
    style={{
      background: "#222",
      padding: "16px",
      color: "#fff",
      textAlign: "center",
      width: "100%",
    }}>
    FATTURE GENERATOR
  </header>
);

export default Header;
